# Conexión a SQL Server (contenedor Docker)
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=TuPasswordSeguro123!
DB_NAME=biblioteca

# Servidor API
API_PORT=8081

# Clave para firmar los tokens JWT (cámbiala en producción)
JWT_SECRET=cambia-esta-clave-secreta-en-produccion
